<div class="container">

  <h1>Create a new Lesson</h1>

<%= simple_form_for @lesson, :url => {:action => 'update', :id => @lesson.id}, :method => 'put', :html => {:class => 'form-horizontal', :multipart => true } do |f| %>

  <%= render(:partial => 'lesson_form_new', :locals => {:f => f}) %>

  <%= f.button :submit, class: "btn btn-primary", value: "Update Lesson" %>
<% end %>

</div>